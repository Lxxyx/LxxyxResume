{
  "name": "resume-generator",
  "version": "2.0.0",
  "description": "Resume generator by Gulp + Sass",
  "main": "gulpfile.js",
  "scripts": {
    "start": "gulp clean && gulp",
    "pdf": "gulp pdf",
    "dev": "gulp dev",
    "deploy": "gulp pdf && gulp deploy && git add -A && git commit -m 'update resume' && git push origin master"
  },
  "author": "Lxxyx",
  "license": "MIT",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-connect": "^5.0.0",
    "gulp-copy": "^0.0.2",
    "gulp-jade": "^1.1.0",
    "gulp-sass": "3.1.0",
    "jade": "^1.11.0",
    "puppeteer": "*",
    "rimraf-promise": "^2.0.0"
  },
  "devDependencies": {
    "gulp-clean": "^0.3.2",
    "gulp-gh-pages": "^0.5.4"
  }
}
